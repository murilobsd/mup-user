pub mod user_publisher_adapter;

pub use user_publisher_adapter::UserPublisherAdapter;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
